# Reconciliation checkpoint

The UI supervisor recovery is being reconciled on top of the latest `main` without history rewrites. Configurable UI-port support is being carried forward separately so it can be validated together with supervisor recovery before promotion.
