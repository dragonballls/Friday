# Friday — Barebones

The intended experience is deliberately simple:

**Talk/type → Friday codes → watch the work.**

The app is not meant to be a showcase of many UI features. The default window should make it immediately obvious what Friday is doing, whether it is making progress, and whether the task succeeded or failed.

The old feature-rich implementation is retained in the repository history and other branches; this branch is the focused runtime.