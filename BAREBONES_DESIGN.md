# Minimal interface

The window should contain only:

- A compact status header: current task + state.
- A scrollable live activity feed.
- A text input and send control.
- A microphone control for speech input.
- A stop/resume control for the current task.

No dashboard, persona carousel, holodeck, 3D scene, feature marketplace, or decorative navigation is required in the default app.